#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

# If you want to use GNOME config tools...
#
#if test -x /usr/lib/openbox/gnome-settings-daemon >/dev/null; then
#  /usr/lib/openbox/gnome-settings-daemon &
#elif which gnome-settings-daemon >/dev/null 2>&1; then
#  gnome-settings-daemon &
#fi

# If you want to use XFCE config tools...
#
#xfce-mcs-manager &

# STARTUP --------------------------------------------------------

wallpaper='03972_paulinalakefromabove_1920x1080.jpg'

homeConfig="/home/dava/.1homepc-config/home/config"

if ls /home/dava/.config/compton.conf &> /dev/null; then
	rm /home/dava/.config/compton.conf
fi
ln -s $homeConfig/compton.conf /home/dava/.config/compton.conf &

compton &

sleep 1s
guake &

feh --bg-scale /home/dava/pictures/wallpapers/new/$wallpaper &

/home/dava/.1homepc-config/home/setlayout 0 3 2 0 &

volumeicon &

numlockx &

opensnap -d &

xset -dpms; xset s off &

tint2 &

sleep 1s
conky &

sleep 1s
gmrun &

sleep 1s
killall gmrun

sleep 2s
deluge &












