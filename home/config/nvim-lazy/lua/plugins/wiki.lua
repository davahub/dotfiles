
return {
  {
    "vimwiki/vimwiki",
    -- config = function()
      -- vim.g.vimwiki_list = {
      --   {
      --     path = "~/workspace/.work/winconfig/reference-md/",
          -- index = "main",
          -- syntax = "markdown",
          -- ext = "md",
          -- auto_diary_index = 1,
          -- auto_generte_links = 0
          -- auto_toc = 1,
        -- }
      -- }
    -- end
  }
}

